version: 2
exported_at: 2026-01-17T22:16:32.250372928-06:00
task:
    id: TASK-367
    title: Create SettingsSidebar component (240px)
    description: |-
        Create the SettingsSidebar component (240px) for settings page navigation.

        REFERENCE FILES:
        - example_ui/settings.html (.settings-nav class, lines 107-163)
        - example_ui/Screenshot_20260116_201916.png (left sidebar in settings)

        TYPESCRIPT INTERFACES:
        ```typescript
        interface SettingsSidebarProps {
          activeSection?: string;
          onSectionChange?: (section: string) => void;
          className?: string;
        }

        interface SettingsNavSection {
          id: string;
          label: string;
          items: SettingsNavItem[];
        }

        interface SettingsNavItem {
          id: string;
          label: string;
          icon: React.ComponentType<{ size?: number }>;
          path: string;
          badge?: string | number;
          badgeColor?: "default" | "primary" | "success" | "warning";
        }

        const sections: SettingsNavSection[] = [
          {
            id: "prompts",
            label: "PROMPTS",
            items: [
              { id: "system", label: "System Prompts", icon: Terminal, path: "/settings/prompts/system" },
              { id: "commands", label: "Commands", icon: Command, path: "/settings/prompts/commands", badge: 12 },
              { id: "templates", label: "Templates", icon: FileText, path: "/settings/prompts/templates" },
            ]
          },
          // ... Configuration, Automation, Advanced sections
        ];

        export const SettingsSidebar: React.FC<SettingsSidebarProps> = ({ activeSection }) => { ... }
        ```

        SUCCESS CRITERIA:
        1. SettingsSidebar component at web/src/components/settings/SettingsSidebar.tsx
        2. Fixed 240px width, full height, var(--bg-card) background
        3. Header with "Settings" title and icon
        4. Grouped navigation sections: Prompts, Configuration, Automation, Advanced
        5. Section labels: uppercase, muted, 9px font
        6. Nav items: icon + label + optional badge
        7. Active state with primary background and border-left accent
        8. npm run typecheck exits 0

        EXACT CSS VALUES (from mockup):
        ```css
        .settings-sidebar {
          width: 240px;
          background: var(--bg-card);
          border-right: 1px solid var(--border);
          padding: 16px 0;
          overflow-y: auto;
        }
        .settings-header {
          padding: 0 16px 16px;
          border-bottom: 1px solid var(--border);
          margin-bottom: 16px;
        }
        .nav-section-label {
          font-size: 9px;
          font-weight: 600;
          text-transform: uppercase;
          letter-spacing: 0.05em;
          color: var(--text-muted);
          padding: 8px 16px;
        }
        .nav-item {
          display: flex;
          align-items: center;
          gap: 10px;
          padding: 8px 16px;
          color: var(--text-secondary);
          transition: all 0.15s ease;
        }
        .nav-item:hover {
          background: var(--bg-surface);
        }
        .nav-item.active {
          background: var(--primary-dim);
          color: var(--primary-bright);
          border-left: 2px solid var(--primary);
        }
        .nav-badge {
          margin-left: auto;
          padding: 2px 6px;
          border-radius: 4px;
          font-size: 10px;
          background: var(--bg-surface);
        }
        .nav-badge.active {
          background: rgba(168, 85, 247, 0.2);
        }
        ```

        ACCESSIBILITY:
        - role="navigation" with aria-label="Settings navigation"
        - aria-current="page" on active item
        - Keyboard navigation with arrow keys within section
        - Focus trap within sidebar

        RESPONSIVE:
        - Below 768px: sidebar collapses to icons only
        - Below 480px: sidebar becomes bottom sheet or hamburger menu

        FULL NAVIGATION STRUCTURE:
        - PROMPTS: System Prompts, Commands (12), Templates
        - CONFIGURATION: Environment, Agents, Hooks, MCP Servers
        - AUTOMATION: Triggers, Schedules, Rules
        - ADVANCED: API Keys, Tokens, Logs, Database

        EDGE CASES:
        - Long section lists scroll within sidebar
        - Badge counts update in real-time
        - Collapsed sections remember state

        FILES TO CREATE:
        - web/src/components/settings/SettingsSidebar.tsx
        - web/src/components/settings/SettingsSidebar.css
    weight: medium
    status: planned
    branch: orc/TASK-367
    queue: active
    priority: high
    category: feature
    initiative_id: INIT-021
    blocked_by:
        - TASK-335
    created_at: 2026-01-16T21:03:03-06:00
    updated_at: 0001-01-01T00:00:00Z
plan:
    version: 1
    task_id: TASK-367
    weight: medium
    description: Medium task - spec, implement, review, test, docs with multi-agent code review
    phases:
        - id: spec
          name: spec
          prompt: |
            Create a specification for this task:

            **Task**: {{TASK_TITLE}}
            **Category**: {{TASK_CATEGORY}}
            **Description**: {{TASK_DESCRIPTION}}

            {{INITIATIVE_CONTEXT}}

            ## Instructions

            Create a clear, actionable specification that defines exactly what needs to be done
            and how to verify it's complete.

            ### 1. Problem Statement
            Summarize what needs to be solved in 1-2 sentences.

            ### 2. Success Criteria (REQUIRED)
            Define specific, testable criteria as checkboxes:
            - Each criterion must be verifiable (file exists, test passes, API returns X)
            - No vague language ("works well", "is fast")
            - Include both functional and quality criteria

            ### 3. Testing Requirements (REQUIRED)
            Specify what tests must pass:
            - [ ] Unit test: [specific test description]
            - [ ] Integration test: [if applicable]
            - [ ] E2E test: [if UI changes]

            ### 4. Scope
            Define boundaries to prevent scope creep:
            - **In Scope**: What will be implemented
            - **Out of Scope**: What will NOT be implemented

            ### 5. Technical Approach
            Brief plan for implementation:
            - Files to modify
            - Key changes in each file

            ### 6. Category-Specific Analysis

            **If this is a BUG (category=bug):**
            - Reproduction Steps: Exact steps to trigger the bug
            - Current Behavior: What happens now (the bug)
            - Expected Behavior: What should happen
            - Root Cause: Where the bug originates (if known)
            - Verification: How to confirm the fix works

            **If this is a FEATURE (category=feature):**
            - User Story: As a [user], I want [feature] so that [benefit]
            - Acceptance Criteria: Specific conditions for feature acceptance

            **If this is a REFACTOR (category=refactor):**
            - Before Pattern: Current code/architecture
            - After Pattern: Target code/architecture
            - Risk Assessment: What could break

            ## Output Format

            Wrap your spec in artifact tags:

            <artifact>
            # Specification: {{TASK_TITLE}}

            ## Problem Statement
            [1-2 sentences]

            ## Success Criteria
            - [ ] [Criterion 1]
            - [ ] [Criterion 2]

            ## Testing Requirements
            - [ ] [Test 1]
            - [ ] [Test 2]

            ## Scope
            ### In Scope
            - [Item]
            ### Out of Scope
            - [Item]

            ## Technical Approach
            [Brief implementation plan]

            ### Files to Modify
            - [file]: [change]

            ## [Category-Specific Section]
            [Include appropriate section based on category]
            </artifact>

            After completing the spec, commit:
            ```bash
            git add -A
            git commit -m "[orc] {{TASK_ID}}: spec - completed"
            ```

            Then output:
            ```
            **Commit**: [SHA]
            <phase_complete>true</phase_complete>
            ```

            If blocked (requirements unclear):
            ```
            <phase_blocked>
            reason: [what's unclear]
            needs: [what clarification is needed]
            </phase_blocked>
            ```
          gate:
            type: auto
          checkpoint: true
          status: pending
        - id: implement
          name: implement
          prompt: |
            Implement the task according to the specification:

            **Task**: {{TASK_TITLE}}
            **Category**: {{TASK_CATEGORY}}

            {{INITIATIVE_CONTEXT}}

            ## Specification

            {{SPEC_CONTENT}}

            {{RETRY_CONTEXT}}

            ## Instructions

            1. Review the spec's success criteria - these are your acceptance criteria
            2. Implement the required changes following the technical approach
            3. Write/update tests alongside code (as specified in Testing Requirements)
            4. Run tests and fix any failures
            5. Self-review against success criteria before completing

            ### Self-Review Checklist
            - [ ] All success criteria from spec addressed
            - [ ] All testing requirements satisfied
            - [ ] Scope boundaries respected (no extra features)
            - [ ] Error handling complete
            - [ ] Code follows project patterns

            Keep iterating until implementation is complete and tests pass.

            After completing, commit:
            ```bash
            git add -A
            git commit -m "[orc] {{TASK_ID}}: implement - completed"
            ```

            When done, output:
            ```
            **Commit**: [SHA]
            <phase_complete>true</phase_complete>
            ```
          depends_on:
            - spec
          gate:
            type: auto
          checkpoint: true
          status: pending
        - id: review
          name: review
          prompt: ""
          depends_on:
            - implement
          gate:
            type: auto
          checkpoint: true
          status: pending
        - id: test
          name: test
          prompt: |
            Test and review the implementation:

            **Task**: {{TASK_TITLE}}
            **Category**: {{TASK_CATEGORY}}

            ## Specification

            {{SPEC_CONTENT}}

            ## Instructions

            1. Run the full test suite
            2. Verify all Testing Requirements from spec are satisfied
            3. Review code for quality issues
            4. Check for edge cases and security issues
            5. Fix any problems found

            ### Verification Against Spec
            Go through each Success Criterion and Testing Requirement from the spec
            and verify it's satisfied.

            Keep iterating until all tests pass and code quality is acceptable.

            After completing, commit:
            ```bash
            git add -A
            git commit -m "[orc] {{TASK_ID}}: test - completed"
            ```

            When done, output:
            ```
            **Commit**: [SHA]
            <phase_complete>true</phase_complete>
            ```
          depends_on:
            - review
          gate:
            type: auto
          checkpoint: true
          status: pending
        - id: docs
          name: docs
          prompt: |
            Update documentation for:

            **Task**: {{TASK_TITLE}}
            **Category**: {{TASK_CATEGORY}}

            ## Specification

            {{SPEC_CONTENT}}

            ## Instructions

            1. Update any relevant documentation files
            2. Ensure CLAUDE.md reflects the changes if applicable
            3. Add/update code comments where needed
            4. Update README if user-facing changes were made

            Keep iterating until documentation is complete.

            After completing, commit:
            ```bash
            git add -A
            git commit -m "[orc] {{TASK_ID}}: docs - completed"
            ```

            When done, output:
            ```
            **Commit**: [SHA]
            <phase_complete>true</phase_complete>
            ```
          depends_on:
            - test
          gate:
            type: auto
          checkpoint: true
          status: pending
state:
    task_id: TASK-367
    current_phase: ""
    current_iteration: 0
    status: pending
    started_at: 0001-01-01T00:00:00Z
    updated_at: 0001-01-01T00:00:00Z
    phases: {}
    tokens:
        input_tokens: 0
        output_tokens: 0
        total_tokens: 0
    cost:
        total_cost_usd: 0
