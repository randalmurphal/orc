version: 2
exported_at: 2026-01-17T22:16:29.373667851-06:00
task:
    id: TASK-424
    title: Create API response helper wrappers
    description: |-
        Create response helper wrappers to reduce repetitive JSON response patterns:

        Current state: Each handler manually calls s.jsonResponse() and s.handleOrcError() with similar patterns repeated 858+ times across 40+ handler files.

        Create api/helpers_response.go with typed response helpers:
        - respondWithTask(w, task, err) - handles task response or error
        - respondWithInitiative(w, initiative, err) - handles initiative response or error
        - respondWithTasks(w, tasks, total, err) - handles task list with pagination
        - respondOK(w) - simple success response
        - respondCreated(w, id) - 201 response with ID

        Migrate a few representative handlers to use new helpers as proof of concept.
        Document pattern in api/CLAUDE.md for future handler development.
    weight: small
    status: planned
    branch: orc/TASK-424
    queue: active
    priority: normal
    category: refactor
    created_at: 2026-01-17T19:03:37-06:00
    updated_at: 0001-01-01T00:00:00Z
plan:
    version: 1
    task_id: TASK-424
    weight: small
    description: Small task - implement with tests, fully automated
    phases:
        - id: implement
          name: implement
          prompt: |
            Implement the following task:

            **Task**: {{TASK_TITLE}}

            **Description**: {{TASK_DESCRIPTION}}

            {{RETRY_CONTEXT}}

            1. Implement the required changes
            2. Write/update tests as needed
            3. Run tests and fix any failures

            Keep iterating until implementation is complete and all tests pass.

            When done, output:
            <phase_complete>true</phase_complete>
          gate:
            type: auto
          checkpoint: true
          status: pending
        - id: test
          name: test
          prompt: |
            Final test verification for: {{TASK_TITLE}}

            **Original task**: {{TASK_DESCRIPTION}}

            1. Run the full test suite
            2. Fix any failures found
            3. Verify edge cases are covered

            Keep iterating until all tests pass.

            When done, output:
            <phase_complete>true</phase_complete>
          depends_on:
            - implement
          gate:
            type: auto
          checkpoint: true
          status: pending
state:
    task_id: TASK-424
    current_phase: ""
    current_iteration: 0
    status: pending
    started_at: 0001-01-01T00:00:00Z
    updated_at: 0001-01-01T00:00:00Z
    phases: {}
    tokens:
        input_tokens: 0
        output_tokens: 0
        total_tokens: 0
    cost:
        total_cost_usd: 0
