id: plan-eval-gpt
name: Plan Eval (GPT 5.4)
description: 'Project-scoped planner evaluation workflow using GPT 5.4 via Codex. No commit or PR side effects.'
completion_action: none
phases:
    - template: plan
      sequence: 0
      provider_override: codex
      model_override: gpt-5.4
