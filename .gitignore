# Build output
/orc
*.exe
bin/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node modules and lockfiles (any location)
node_modules/
package-lock.json

# Web build artifacts
web/.svelte-kit/
web/build/
web/tsconfig.tsbuildinfo

# Test artifacts
coverage.out
*.test

# Playwright test artifacts
web/playwright-report/
web/test-results/

# Claude Code local state
.claude/*.local.md

# Playwright MCP screenshots
.playwright-mcp/

# Go workspace (local development only)
go.work
go.work.sum
internal/api/static/*
!internal/api/static/.gitkeep

# orc - Claude Code Task Orchestrator
# All runtime state (DB, worktrees, exports) lives in ~/.orc/, not here.
.mcp.json
.claude/settings.json

/test-results/

# QA artifacts (should go to /tmp/orc-qa-*, but gitignore as safety net)
QA-REPORT*
QA-FINDINGS*
qa-report*
qa-findings*

# Stray spec files created by Claude Code during spec phase
# (specs are stored in the database, not as files)
spec.md
/artifacts/spec.md
# But allow the system prompt template
!templates/system_prompts/spec.md
