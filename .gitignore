# Build output
/orc
*.exe
bin/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node modules and lockfiles (any location)
node_modules/
package-lock.json

# Web build artifacts
web/.svelte-kit/
web/build/
web/tsconfig.tsbuildinfo

# Test artifacts
coverage.out
*.test

# Playwright test artifacts
web/playwright-report/
web/test-results/

# Claude Code local state
.claude/*.local.md

# Playwright MCP screenshots
.playwright-mcp/

# Orc worktrees (parallel task execution)
.orc/worktrees/

# Orc data directories (synced via CR-SQLite, not git)
.orc/tasks/
.orc/initiatives/

# Nested .orc directories (test artifacts, subdirectories)
# Use **/ to match anywhere in the tree
**/.orc/orc.db
**/.orc/orc.db-*
**/.orc/tasks/
**/.orc/initiatives/

# Go workspace (local development only)
go.work
go.work.sum
internal/api/static/*
!internal/api/static/.gitkeep
.orc/orc.db

# orc - Claude Code Task Orchestrator
# SQLite WAL files (regenerated on open)
.orc/orc.db-journal
.orc/orc.db-wal
.orc/orc.db-shm
.mcp.json
.claude/settings.json

/test-results/

# QA artifacts (should go to /tmp/orc-qa-*, but gitignore as safety net)
QA-REPORT*
QA-FINDINGS*
qa-report*
qa-findings*

# Stray spec files created by Claude Code during spec phase
# (specs are stored in the database, not as files)
spec.md
/artifacts/spec.md
# But allow the system prompt template
!templates/system_prompts/spec.md
